sduoj:
  filesys:
    baseDir:  /root/sduoj/filesys

spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

auth-service.ribbon.listOfServers: ${AUTH_SERVICE_URL}
